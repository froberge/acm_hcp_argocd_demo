---
# Dedicated namespace for the Grafana Community Operator and instance.
# All Grafana operator CRs (Grafana, GrafanaDataSource, GrafanaDashboard)
# live here, scoped to a single-namespace operator install.
apiVersion: v1
kind: Namespace
metadata:
  name: grafana-slo
  labels:
    app.kubernetes.io/part-of: coffeeshop
    app.kubernetes.io/component: observability
